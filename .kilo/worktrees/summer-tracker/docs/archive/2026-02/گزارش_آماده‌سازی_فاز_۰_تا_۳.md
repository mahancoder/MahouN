# گزارش آماده‌سازی فاز ۰ تا ۳
## پاکسازی Core - جداسازی Infrastructure از Domain Logic

**تاریخ**: ۱۴۰۴/۱۱/۲۹  
**وضعیت**: ✅ آماده اجرا

---

## خلاصه اجرایی

سیستم کامل برای جداسازی ایمن و تدریجی کد Infrastructure از `mahoun/core/` ایجاد شد. تمام ابزارها production-ready و تست‌شده هستند.

---

## ابزارهای ایجاد شده

### 1. اسکریپت‌های اصلی

| اسکریپت | وظیفه | وضعیت |
|---------|-------|--------|
| `backup_core.py` | پشتیبان‌گیری اتمیک با checksum | ✅ |
| `restore.py` | بازگردانی با safety backup | ✅ |
| `validate_phase.py` | اعتبارسنجی جامع هر فاز | ✅ |
| `execute_phase.py` | اجرای خودکار فازها | ✅ |
| `update_imports.py` | به‌روزرسانی import با AST | ✅ |
| `phase_operations.py` | کتابخانه عملیات قابل استفاده مجدد | ✅ |
| `execute_phases_0_to_3.py` | اجرای کامل فاز ۰-۳ | ✅ |

### 2. تست‌ها

- `test_phase_operations.py`: تست جامع تمام عملیات
- Coverage: Unit + Integration + Idempotency tests

---

## فازهای آماده

### فاز ۰: آماده‌سازی ✅
- **ریسک**: صفر
- **مدت**: ۳۰ دقیقه
- **عملیات**: بررسی وجود اسکریپت‌ها و ایجاد دایرکتوری backup

### فاز ۱: ایجاد دایرکتوری‌ها ✅
- **ریسک**: خیلی کم
- **مدت**: ۵ دقیقه
- **عملیات**: ایجاد `mahoun/infrastructure/` و زیرشاخه‌ها

### فاز ۲: کپی فایل‌ها ✅
- **ریسک**: خیلی کم
- **مدت**: ۱۰ دقیقه
- **عملیات**: کپی `health_cache.py`، `metrics/`، `monitoring/`

### فاز ۳: اضافه کردن Deprecation ✅
- **ریسک**: کم
- **مدت**: ۱۰ دقیقه
- **عملیات**: اضافه کردن هشدار deprecation به فایل‌های قدیمی

---

## نحوه اجرا

### روش ۱: اجرای خودکار (توصیه می‌شود)

```bash
# Dry run ابتدا
python scripts/execute_phases_0_to_3.py --dry-run

# اجرای واقعی با auto-commit
python scripts/execute_phases_0_to_3.py --auto-commit
```

### روش ۲: اجرای دستی هر فاز

```bash
# فاز ۰
python scripts/execute_phase.py 0

# فاز ۱
python scripts/execute_phase.py 1
python scripts/validate_phase.py 1

# فاز ۲
python -c "from scripts.phase_operations import Phase2Operations; \
Phase2Operations.copy_health_cache(); \
Phase2Operations.copy_metrics_module(); \
Phase2Operations.copy_monitoring_module()"

# فاز ۳
python -c "from scripts.phase_operations import Phase3Operations; \
Phase3Operations.add_deprecation_to_health_cache(); \
Phase3Operations.add_deprecation_to_metrics(); \
Phase3Operations.add_deprecation_to_monitoring()"
```

---

## مکانیزم‌های ایمنی

### ۱. پشتیبان‌گیری خودکار
```bash
python scripts/backup_core.py
```

### ۲. Rollback در صورت خطا
```bash
# گزینه ۱: بازگردانی از backup
python scripts/restore.py backups/core_backup_*.tar.gz

# گزینه ۲: Git rollback
git reset --hard PHASE_N_START
```

### ۳. Dry-run برای تمام عملیات
همه اسکریپت‌ها از `--dry-run` پشتیبانی می‌کنند.

---

## معیارهای موفقیت

- ✅ تمام تست‌ها pass شوند (۱۰۰٪)
- ✅ هیچ تغییری در عملکرد نباشد
- ✅ هم import قدیم و هم جدید کار کنند
- ✅ Coverage کاهش نیابد

---

## فازهای بعدی (۴-۱۱)

| فاز | عملیات | ریسک | وضعیت |
|-----|--------|------|--------|
| ۴ | به‌روزرسانی import در mahoun/ | متوسط | 🟡 منتظر |
| ۵ | به‌روزرسانی import در api/ | متوسط | 🟡 منتظر |
| ۶ | به‌روزرسانی import در tests/ | کم | 🟡 منتظر |
| ۷ | حذف فایل‌های deprecated | زیاد | 🔴 منتظر |
| ۸-۱۱ | انتقال LLM، RAG، health_checker | زیاد | 🔴 منتظر |

---

## نکات مهم

1. **همیشه ابتدا dry-run اجرا کنید**
2. **قبل از هر فاز backup بگیرید**
3. **بعد از هر فاز validate کنید**
4. **سیستم بعد از هر فاز کاملاً کاربردی است**
5. **می‌توانید در هر نقطه متوقف شوید**

---

## زمان‌بندی کلی

- **فاز ۰-۳**: ۱ ساعت (آماده اجرا)
- **فاز ۴-۶**: ۸-۱۲ ساعت
- **فاز ۷-۱۱**: ۱۲-۲۰ ساعت
- **جمع**: ۲۴-۳۲ ساعت در ۲-۴ هفته

---

## دستور اجرای سریع

```bash
# اجرای کامل فاز ۰-۳ با یک دستور
python scripts/execute_phases_0_to_3.py --auto-commit

# در صورت موفقیت:
# ✅ Infrastructure directories ایجاد شده
# ✅ فایل‌ها کپی شده
# ✅ Deprecation warnings اضافه شده
# ✅ تمام تست‌ها pass
# ✅ Git tags ایجاد شده
```

---

**نتیجه**: سیستم کاملاً آماده است. می‌توانید با اطمینان فاز ۰-۳ را اجرا کنید.
