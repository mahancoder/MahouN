--- a/api/routers/ingest.py
+++ b/api/routers/ingest.py
@@ -65,10 +65,10 @@ async def get_ingestion_pipeline():
             
         except ImportError as e:
             logger.error(f"Could not import IngestionPipeline: {e}")
-            _ingestion_pipeline: Optional[Any] = None
+            _ingestion_pipeline = None
         except Exception as e:
             logger.error(f"Could not initialize IngestionPipeline: {e}")
-            _ingestion_pipeline: Optional[Any] = None
+            _ingestion_pipeline = None
     return _ingestion_pipeline
 
 

--- a/mahoun/guardrails/runtime_invariants.py
+++ b/mahoun/guardrails/runtime_invariants.py
@@ -27,7 +27,7 @@ def register_node(node_id: str, node: Any) -> None:
 def clear_registry() -> None:
     """Clear the node registry (for testing)"""
     global _node_registry
-    _node_registry: Dict[str, Any] = {}
+    _node_registry = {}
 
 def get_registry() -> Dict[str, Any]:
     """Get the current node registry"""

--- a/mahoun/orchestrator/demo_mvp.py
+++ b/mahoun/orchestrator/demo_mvp.py
@@ -24,7 +24,7 @@ import json
 import logging
 import sys
 from pathlib import Path
-from typing import Any, Dict, List
+from typing import Any, Dict, List, Optional
 from datetime import datetime
 
 # Setup logging
@@ -43,8 +43,8 @@ def print_section(title: str, char: str = "="):
 
 
 # Shared instances for demo (to preserve state across phases)
-_shared_vector_store = None
-_shared_embedding_service = None
+_shared_vector_store: Optional[Any] = None
+_shared_embedding_service: Optional[Any] = None
 
 
 async def run_mvp_demo(

--- a/mahoun/retrieval/gat_reranker.py
+++ b/mahoun/retrieval/gat_reranker.py
@@ -227,18 +227,18 @@ class GATRerankerModel(nn.Module):
             Returns:
                 (mean_scores, std_scores)
             """
-        self.train()  # Enable dropout
-        
-        predictions_list: List[Any] = []
-        for _ in range(num_samples):
-            with torch.no_grad():
-                scores = self.forward(x, edge_index, edge_attr)
-                predictions_list.append(scores)
-        
-        predictions = torch.stack(predictions_list)  # type: ignore[attr-defined]
-        mean_scores = predictions.mean(dim=0)  # type: ignore[attr-defined]
-        std_scores = predictions.std(dim=0)  # type: ignore[attr-defined]
-        
-        self.eval()
-        
-        return mean_scores, std_scores
+            self.train()  # Enable dropout
+            
+            predictions_list: List[Any] = []
+            for _ in range(num_samples):
+                with torch.no_grad():
+                    scores = self.forward(x, edge_index, edge_attr)
+                    predictions_list.append(scores)
+            
+            predictions = torch.stack(predictions_list)  # type: ignore[attr-defined]
+            mean_scores = predictions.mean(dim=0)  # type: ignore[attr-defined]
+            std_scores = predictions.std(dim=0)  # type: ignore[attr-defined]
+            
+            self.eval()
+            
+            return mean_scores, std_scores

--- a/api/routers/mahoun.py
+++ b/api/routers/mahoun.py
@@ -165,11 +165,11 @@ def get_orchestrator():
     description="Upload documents (PDF, DOCX, TXT, Images) and normalize them"
 )
 async def upload_documents(
+    background_tasks: BackgroundTasks,
     file: UploadFile = File(...),
     doc_type: Optional[str] = None,
     metadata: Optional[Dict[str, Any]] = None,
-    index: bool = True,
-    background_tasks: Optional[BackgroundTasks] = None
+    index: bool = True
 ):
     """
     Upload and process documents

